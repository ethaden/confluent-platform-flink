bootstrap.servers=localhost:12091,localhost:12092,localhost:12093
#security.protocol=SASL_SSL
sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username='superUser' password='superUser';
sasl.mechanism=PLAIN
# Required for consumers only:
#group.id=<Name of your consumer group>

# Schema Registry
schema.registry.url=localhost:8085
basic.auth.credentials.source=USER_INFO
basic.auth.user.info=superUser:superUser
